<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>AI Calculator | Quiz Game</title>
  <meta name="description" content="Play this smart quiz game powered by AI Calculator. Boost your brain with fun and tricky questions.">
  <meta name="keywords" content="quiz game, AI calculator, trivia, gk, logic quiz, fun quiz, online quiz">
<link rel="canonical" href="https://smartaicalculator.com/quize-exam.html">
  <meta name="author" content="AI Calculator Team">
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      margin: 0;
      padding: 0;
      background: linear-gradient(to right, #0f2027, #203a43, #2c5364);
      color: white;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
      text-align: center;
    }

    h1 {
      margin-top: 30px;
      font-size: 2.5rem;
    }

    h2 {
      margin-bottom: 20px;
      color: #ffcc00;
    }

    #quiz-container {
      background: #1e2a38;
      padding: 30px;
      border-radius: 15px;
      width: 90%;
      max-width: 500px;
      box-shadow: 0 0 20px #00000070;
    }

    #question {
      font-size: 1.3rem;
      margin-bottom: 20px;
    }

    .option {
      background-color: #324b5f;
      border: none;
      border-radius: 10px;
      padding: 10px;
      margin: 8px 0;
      font-size: 1rem;
      cursor: pointer;
      transition: 0.2s;
      color: white;
      width: 100%;
    }

    .option:hover {
      background-color: #ffcc00;
      color: black;
    }

    #timer, #score {
      margin: 10px;
      font-weight: bold;
    }

    #result {
      margin-top: 15px;
      font-size: 1.1rem;
    }
  </style>
</head>
<body>

  <h1>AI CALCULATOR</h1>
  <h2>Quiz Game</h2>

  <div id="quiz-container">
    <div id="score">Score: 0</div>
    <div id="timer">Time Left: 15s</div>
    <div id="question">Loading question...</div>
    <div id="options"></div>
    <div id="result"></div>
  </div>

  <script>
    const questions = [
      {
        q: "What is the capital of France?",
        options: ["Berlin", "Madrid", "Paris", "Rome"],
        answer: "Paris"
      },
      {
        q: "Which planet is known as the Red Planet?",
        options: ["Earth", "Venus", "Mars", "Jupiter"],
        answer: "Mars"
      },
      {
        q: "5 + 7 × 2 = ?",
        options: ["24", "19", "26", "17"],
        answer: "19"
      },
      {
        q: "Who invented the light bulb?",
        options: ["Tesla", "Newton", "Edison", "Einstein"],
        answer: "Edison"
      },
      {
        q: "What is H2O commonly known as?",
        options: ["Hydrogen", "Water", "Salt", "Oxygen"],
        answer: "Water"
      },
      {
        q: "Which is the largest organ in the human body?",
        options: ["Liver", "Skin", "Heart", "Lungs"],
        answer: "Skin"
      },
      {
        q: "Ravi has ₹100. Buys a pen for ₹30. How much left?",
        options: ["₹30", "₹70", "₹60", "₹50"],
        answer: "₹70"
      }
    ];

    let score = 0;
    let timer;
    let timeLeft = 15;
    let currentQuestionIndex = 0;

    const questionEl = document.getElementById("question");
    const optionsEl = document.getElementById("options");
    const timerEl = document.getElementById("timer");
    const scoreEl = document.getElementById("score");
    const resultEl = document.getElementById("result");

    function shuffle(array) {
      return array.sort(() => Math.random() - 0.5);
    }

    const quizData = shuffle([...questions]);

    function showQuestion() {
      if (currentQuestionIndex >= quizData.length) {
        questionEl.textContent = "🎉 Quiz Over!";
        optionsEl.innerHTML = "";
        resultEl.textContent = `Final Score: ${score}/${quizData.length}`;
        clearInterval(timer);
        return;
      }

      timeLeft = 15;
      timerEl.textContent = `Time Left: ${timeLeft}s`;
      clearInterval(timer);
      timer = setInterval(() => {
        timeLeft--;
        timerEl.textContent = `Time Left: ${timeLeft}s`;
        if (timeLeft === 0) {
          clearInterval(timer);
          resultEl.textContent = `⏱️ Time's up! Correct: ${quizData[currentQuestionIndex].answer}`;
          currentQuestionIndex++;
          setTimeout(showQuestion, 1500);
        }
      }, 1000);

      const q = quizData[currentQuestionIndex];
      questionEl.textContent = q.q;
      optionsEl.innerHTML = "";
      shuffle(q.options).forEach(option => {
        const btn = document.createElement("button");
        btn.classList.add("option");
        btn.textContent = option;
        btn.onclick = () => checkAnswer(option);
        optionsEl.appendChild(btn);
      });
      resultEl.textContent = "";
    }

    function checkAnswer(selected) {
      clearInterval(timer);
      const correct = quizData[currentQuestionIndex].answer;
      if (selected === correct) {
        score++;
        scoreEl.textContent = `Score: ${score}`;
        resultEl.textContent = "✅ Correct!";
      } else {
        resultEl.textContent = `❌ Incorrect! Correct: ${correct}`;
      }
      currentQuestionIndex++;
      setTimeout(showQuestion, 1500);
    }

    showQuestion();
  </script>

</body>
</html>
